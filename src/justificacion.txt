Siguiendo el principio de abstraer lo que cambia, el comportamiento de transferencia
por protocolo y parseo de datos por formato se abstrajeron en forma de interfaces que
permiten utilizar implementaciones concretas de estos mismos.

Cada banco implementa un método de transfer distinto, con el que envía una transferencia
con los datos requeridos.
